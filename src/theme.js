import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            // main: '#e40046',
            main: '#19727e'
        },
        secondary: {
            main: '#ff6d05'
        },
        white : {
            main: '#ffffff'
        }
    }
})